# 🧵 Java Thread Pools — шпаргалка

---

## 1️⃣ Основы

- Thread Pool — это пул потоков, который **переиспользует потоки** для выполнения задач.
- Цель:
    - Избежать постоянного создания/удаления потоков.
    - Контролировать количество одновременно работающих потоков.
    - Улучшить производительность в многопоточных приложениях.

- В Java используются интерфейсы:
    - `Executor` — базовый интерфейс для выполнения задач.
    - `ExecutorService` — расширяет Executor, поддерживает shutdown, submit, invokeAll.
    - `ScheduledExecutorService` — поддерживает периодическое выполнение.

---

## 2️⃣ Основные виды пулов (Executors)

| Тип пула | Особенности | Пример |
|----------|-------------|--------|
| `FixedThreadPool(n)` | Постоянное количество потоков. Очередь задач | `Executors.newFixedThreadPool(4)` |
| `CachedThreadPool` | Динамический пул, создаёт новые потоки по мере необходимости, удаляет неиспользуемые через 60 сек | `Executors.newCachedThreadPool()` |
| `SingleThreadExecutor` | Всегда один поток, задачи выполняются последовательно | `Executors.newSingleThreadExecutor()` |
| `ScheduledThreadPool` | Поддержка задержки и периодического выполнения | `Executors.newScheduledThreadPool(2)` |

---

## 3️⃣ Основные методы ExecutorService

| Метод | Назначение |
|-------|------------|
| `submit(Runnable/Callable)` | Отправка задачи на выполнение, возвращает Future |
| `invokeAll(Collection<Callable>)` | Выполнить несколько задач и дождаться завершения |
| `invokeAny(Collection<Callable>)` | Выполнить несколько задач и вернуть результат самой быстрой |
| `shutdown()` | Остановка пула, новые задачи не принимаются |
| `shutdownNow()` | Попытка немедленно завершить все задачи, прерывание активных потоков |
| `isShutdown()` | Проверка, что shutdown вызван |
| `isTerminated()` | Проверка, что все задачи завершены после shutdown |

---

## 4️⃣ Примеры использования

### FixedThreadPool

```java
ExecutorService executor = Executors.newFixedThreadPool(3);

for (int i = 0; i < 5; i++) {
    int taskId = i;
    executor.submit(() -> {
        System.out.println("Task " + taskId + " running in " + Thread.currentThread().getName());
    });
}

executor.shutdown();
