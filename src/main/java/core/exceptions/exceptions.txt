Если исключение выброшено в блоке cath то в других catch оно не поймается (ловится только то что в try)

-- Exit code — целое число, это в ОС есть для всех потоков
Обычно используется диапазон 0–255
0 → success