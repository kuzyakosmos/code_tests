# 🧭 План подготовки к собеседованию Java Developer (5Post)

## 📅 7-дневный план

---

### 🗓️ День 1 — Core Java и ООП

**Цель:** освежить основы и принципы, часто встречающиеся в блиц-вопросах.

**Темы:**
- SOLID (особенно LSP, DIP)
- Принцип подстановки Барбары Лисков
- Разница между интерфейсом и абстрактным классом
- Инкапсуляция, наследование, полиморфизм, композиция
- Модификаторы доступа (в том числе `private class`)
- Паттерны: Builder, Factory, Singleton
- Wrapper vs Primitive (nullability, autoboxing)
- `String`, `StringBuilder`, `StringBuffer`
- String pool, immutability, thread-safety

**Практика:**
- Реализовать `Builder` вручную
- Пример нарушения LSP
- Сравнить `String` vs `StringBuilder` vs `StringBuffer`

---

### 🗓️ День 2 — Коллекции и Stream API

**Цель:** уверенно отвечать на вопросы по стандартной библиотеке.

**Темы:**
- Коллекции: `HashMap`, `HashSet`, `LinkedHashMap`, `ConcurrentHashMap`
- Требования к ключу `HashMap` (`equals()`, `hashCode()`)
- Коллизии и расчёт bucket'ов
- Stream API:
    - **Intermediate** vs **Terminal** операции
    - Ленивая модель вычислений (без терминальной операция стрим не выполнится)
    - Параллельные стримы (`parallelStream()`)

**Практика:**
- Написать несколько стримов, включая параллельные
- Понять, где параллельные стримы вредят производительности

---

### 🗓️ День 3 — JMM

---

### 🗓️ День 4 — Многопоточность и Concurrency

**Цель:** подготовиться к вопросам о потоках, атомарности и пулах.

**Темы:**
- `volatile`, `synchronized`, `ReentrantLock`
- Почему операция инкремента `i++` не атомарна
- Как достичь атомарности: `AtomicInteger`, `synchronized`, `Lock`
- Пулы потоков:
    - `FixedThreadPool`
    - `CachedThreadPool`
    - `ForkJoinPool`
    - `VirtualThreadPerTaskExecutor` (Java 21)
- `Future` vs `CompletableFuture`
- Virtual threads и structured concurrency

**Практика:**
- Пример с `CompletableFuture`: цепочка thenApply, thenCombine
- Демонстрация `AtomicInteger` vs `int` при многопоточности

---

### 🗓️ День 5 — Spring Framework

**Цель:** повторить DI, жизненный цикл бинов и транзакции.

**Темы:**
- Bean lifecycle (инициализация → использование → уничтожение)
- Скоупы бинов: singleton, prototype, request, session
- Как внедрить `prototype` в `singleton`: `ObjectFactory`, `Provider`, `@Lookup`
- `@Transactional`: propagation, isolation, rollback rules
- `@Configuration`, `@ComponentScan`, `@Bean`
- Контекст приложения и lifecycle callbacks

**Практика:**
- Мини-приложение с `prototype` внутри `singleton`
- Логи `@PostConstruct` / `@PreDestroy`

---

### 🗓️ День 6 — Kafka и распределённые системы

**Цель:** уверенно отвечать на вопросы по Kafka и паттернам согласования.

**Темы:**
- Kafka: Producer, Consumer, partition, offset, rebalance
- Примеры:
    - 3 partition → 4 consumer ⇒ 1 consumer простаивает
    - 3 partition → 2 consumer ⇒ по ~1.5 partition на каждого
- Exactly-once, idempotency, retry, DLQ
- Outbox pattern, Saga pattern (choreography vs orchestration)
- Temporal / Camunda: workflows, retries, compensation

**Практика:**
- Нарисовать схему топика с consumers
- Объяснить различие orchestration и choreography

---

### 🗓️ День 7 — Базы данных (PostgreSQL)

**Цель:** уметь говорить о производительности и структуре данных.

**Темы:**
- Индексы: B-tree, GIN, GiST
- Composite indexes и partial indexes
- `EXPLAIN ANALYZE`: план выполнения запроса
- Seq Scan vs Index Scan
- Isolation levels, deadlocks
- Connection pool (HikariCP)
- Оптимизация при высокой утилизации БД

**Практика:**
- Пример `EXPLAIN ANALYZE`
- Разбор, когда индекс не используется

---

### 🗓️ День 8 — Kubernetes и CI/CD

**Цель:** понимать деплой, скейлинг и наблюдаемость.

**Темы:**
- Kubernetes: Pod, Deployment, Service, Ingress
- Liveness/Readiness probes, graceful shutdown
- ConfigMap, Secret
- Argo CD, GitLab CI/CD pipeline
- ELK stack, Prometheus, Micrometer (в Spring Boot)
- Логирование и мониторинг микросервисов

**Практика:**
- Описать деплой Spring Boot в K8s
- Объяснить zero-downtime deployment

---

## 🎯 Общие рекомендации

- Каждый день: **2 часа теории + 1 час практики**
- Повторяй ответы вслух, будто объясняешь коллеге
- Последний день — блиц по всем темам
- Если время позволяет — реши пару задач по коду и Kafka

---

## 📚 Ресурсы для повторения

- *Effective Java* (Joshua Bloch)
- *Java Concurrency in Practice* (Brian Goetz)
- *Spring in Action* (Craig Walls)
- *Designing Data-Intensive Applications* (Martin Kleppmann)
- Temporal.io Java SDK samples
