Chain of Responsibility

Паттерн COR (Chain of Responsibility) — Цепочка обязанностей — это поведенческий шаблон проектирования, который позволяет передавать запрос по цепочке обработчиков, пока один из них не сможет его обработать.

Используется, когда:
есть несколько обработчиков одного типа запрос, порядок обработки важен,
отправитель запроса не должен знать, кто именно его обработает.

🧩 Основная идея
Вместо того чтобы один объект решал всё, создаётся цепочка обработчиков, каждый из которых:
либо обрабатывает запрос,
либо передаёт его дальше по цепочке.
Это уменьшает связанность и делает систему легко расширяемой.

📌 Структура паттерна
Компоненты:
Handler (Обработчик)
Абстрактный класс или интерфейс:
- содержит ссылку на следующий обработчик
- метод handle(request)

Concrete Handler (Конкретные обработчики)
- каждый обрабатывает запрос по-своему
- решает: обработать или передать дальше

Client (Клиент)
- создаёт цепочку
- отправляет запрос первому обработчику