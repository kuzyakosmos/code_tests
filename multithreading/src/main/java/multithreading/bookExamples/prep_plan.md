# Java Concurrency – Подготовка к собеседованию

## 🔹 Этап 1. Краткое повторение ключевых тем

### 🧵 Основы
- Что такое **Thread**, **Runnable**, **Callable**, **Future**
- Разница между `start()` и `run()`
- Что делает `join()`
- Что такое **race condition**
- Что делает **volatile**
- Что такое **synchronized** (на чем можно синхронизироваться, что нельзя)

### 🧩 Более продвинутое
- **Locks / ReentrantLock**, отличие от synchronized
- **Condition** и **await/signal**
- **Atomic** классы и CAS
- **ExecutorService** / **ThreadPoolExecutor**
- **ForkJoinPool**, **CompletableFuture**
- **ConcurrentHashMap**, **CopyOnWriteArrayList**
- **Semaphore**, **CountDownLatch**, **CyclicBarrier**, **Phaser**

### ⚙️ Теоретические концепции
- **Happens-before**, **Memory visibility**
- **Deadlock**, **Livelock**, **Starvation**
- **Immutable объекты и потокобезопасность**

---

## 🔹 Этап 2. Типовые задачки (по возрастанию сложности)

| Уровень | Пример задачи | Что проверяет |
|----------|----------------|----------------|
| 🟢 easy | Реализовать поток, печатающий числа 1–10, с использованием `Thread` | основы работы с потоками |
| 🟢 easy | Два потока: один печатает "Ping", другой — "Pong" по очереди | synchronized, wait/notify |
| 🟡 medium | Написать **Thread-safe Singleton** тремя способами | volatile, synchronized, static inner class |
| 🟡 medium | Реализовать свой **BlockingQueue** | wait/notify, синхронизация |
| 🟡 medium | Реализовать **Producer–Consumer** | coordination, locks |
| 🟠 hard | Написать **обед философов** | deadlock prevention |
| 🟠 hard | Реализовать **реактивную цепочку CompletableFuture** | async pipeline, thread pools |
| 🔴 expert | Мини-реализация **ThreadPoolExecutor** | queue, worker threads, shutdown |

---

## 🔹 Этап 3. Устные вопросы для собеса
- Что делает `volatile` и когда он **не гарантирует атомарность**?
- Чем `ReentrantLock` лучше `synchronized`?
- Что такое **spurious wakeup**?
- Почему `ConcurrentHashMap` не блокирует весь map при изменении?
- Что делает `ForkJoinPool`?
- Как работает `CompletableFuture.thenCompose()`?

---

## 🔹 Рекомендации по практическому этапу
1. Начать с простых задач на `Thread` и `Runnable`.
2. Потом перейти к задачам с `wait/notify` (Ping-Pong).
3. Реализовать **thread-safe Singleton** тремя способами.
4. Перейти к **Producer–Consumer** и **BlockingQueue**.
5. Для продвинутых: **ForkJoinPool**, **CompletableFuture**, **ThreadPoolExecutor**.  
